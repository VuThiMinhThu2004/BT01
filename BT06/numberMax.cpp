#include <iostream>
#include <string>
#include <algorithm>

void Try(int n, )




/*
#include "bits/stdc++.h"
using namespace std;

void Try(int s, set<int> &number) {
    if (s == 0) {
        for (auto it:number) cout << it;
        return;
    }
    for (int i = 0; i <= 9; i++) {
        if (number.count(i) == 0 && (s > 2*i)) {
            number.insert(i);
            Try(s-i,number);
            s += i;
            number.erase(i);
        }
    }
    
}
int main() {
    int n; cin >> n;
    if (n > 45) {
        cout << "NONE";
        return 0;
    }
    set<int> number;

    Try(n,number);
}
*/